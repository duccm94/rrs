<%= form_for [book, review, comment], url: restaurant_review_comments_url(book, review, comment), method: :post do |f| %>
  <%= f.hidden_field :user_id, value: current_user.id %>
  <%= f.text_field :content, class: "form-control" %>
<% end %>
