<header class="navbar navbar-fixed-top header-bar">
  <div class="container">
    <%= link_to t("app.bookapp"), root_path, id: "logo" %>
    <nav>
      <ul class="nav navbar-nav navbar-right">

        <% if logged_in? %>
          <% if current_user.is_admin? %>
            <li><%= link_to t("app.users"), admin_users_path %></li>
            <li><%= link_to t("app.categories"), admin_categories_path %></li>
            <li><%= link_to "Restaurant", admin_restaurants_path %></li>
            <li><%= link_to "Requests", admin_requests_path %></li>
          <% else %>
            <li><%= link_to "Restaurant", restaurants_path %></li>
            <li><%= link_to t("app.user"), users_path %></li>
            <li><%= link_to t("app.requests"), requests_path %>
          <% end %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <%= current_user.name %><b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to t("app.profile"), current_user %></li>
              <li><%= link_to t("app.set"), edit_user_path(current_user) %></li>
              <li>
                <%= link_to t("app.logout"), logout_path, method: "delete" %>
              </li>
              <li class="divider"></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to t("login"), login_path %></li>
          <li> <%= link_to t("views.static_pages.signup"), signup_path %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>
