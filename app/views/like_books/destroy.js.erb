$('#book_<%=@book.id%>-like_button').html('<%= escape_javascript(
  render partial: "like_books/like_button",
  locals: {book: @book}
  ) %>');
$('#book_<%=@book.id%>-like_stats')
  .html('<%= @book.like_books.count %>');
